name: Generate Stats

on:
  workflow_dispatch:

jobs:
  stats:
    runs-on: ubuntu-latest
    
    steps:
    - name: Maximize build space
      uses: easimon/maximize-build-space@master
      with:
        root-reserve-mb: 15360
        swap-size-mb: 1024
        remove-dotnet: 'true'
        remove-android: 'true'
        remove-haskell: 'true'
        remove-codeql: 'true'
        remove-docker-images: 'true'
        
    - name: Checkout main repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 1
    
    - name: Clone tziakcha_record_miner repository
      run: |
        git clone https://github.com/tziakcha-stats/tziakcha_record_miner.git
        cd tziakcha_record_miner
        git submodule update --init --recursive
    
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake build-essential
    
    - name: Build record miner
      run: |
        cd tziakcha_record_miner
        mkdir -p build
        cd build
        cmake .. -DCMAKE_BUILD_TYPE=Release
        make -j$(nproc)
    
    - name: Prepare data directories
      run: |
        mkdir -p tziakcha_record_miner/data
        mkdir -p tziakcha_record_miner/data/player
        rm -rf tziakcha_record_miner/data/record || true
        
        if [ -d "records" ]; then
          ln -s $(pwd)/records $(pwd)/tziakcha_record_miner/data/record
          echo "Success: Linked $(pwd)/records to tziakcha_record_miner/data/record"
        else
          echo "Error: records directory not found"
          exit 1
        fi

        ls -alF tziakcha_record_miner/data/
    
    - name: Run stats analysis
      run: |
        cd tziakcha_record_miner
        ./build/src/analyzer/stats_cli \
          --dir ./data/record/ \
          --player-stats \
          --player-dir data/player
        
        echo "Generated player stats:"
        ls -lh data/player/ || echo "No player stats generated"
    
    - name: Copy player stats to repository root
      run: |
        mkdir -p player
        
        if [ -d "tziakcha_record_miner/data/player" ]; then
          cp -rf tziakcha_record_miner/data/player/* player/ || true
          echo "Copied player stats"
        fi
        
        echo "Player stats files:"
        ls -lh player/ || echo "No player stats files"
    
    - name: Configure Git
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
    
    - name: Commit and push changes
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        git add player/
        
        if git diff --staged --quiet; then
          echo "No changes to commit"
          exit 0
        fi

        git commit -m "Auto-generate player stats"

        git pull --rebase
        if [ $? -ne 0 ]; then
          echo "Rebase failed due to conflicts. Please resolve manually."
          exit 1
        fi

        git push
    
    - name: Cleanup
      if: always()
      run: |
        rm -rf tziakcha_record_miner

  rank:
    needs: stats
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout main repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 1
    
    - name: Clone tziakcha_record_miner repository
      run: |
        git clone https://github.com/tziakcha-stats/tziakcha_record_miner.git
    
    - name: Prepare data directories
      run: |
        git pull
        
        mkdir -p tziakcha_record_miner/data/rank
        
        if [ -d "player" ]; then
          ln -s player tziakcha_record_miner/data/
          echo "Copied player data to miner directory"
        else
          echo "No player directory found"
        fi
        
        echo "Player files:"
        ls -lh tziakcha_record_miner/data/player/ || echo "No player data"
    
    - name: Generate player rankings
      run: |
        cd tziakcha_record_miner
        
        bash ./ci/stats/rank_players.sh stats.average_step_seconds -p ./data/player -r ./data/rank
        
        bash ./ci/stats/rank_players.sh current_elo -p ./data/player -r ./data/rank
        
        bash ./ci/stats/rank_players.sh stats.avg_starting_shanten_13 -p ./data/player -r ./data/rank
        bash ./ci/stats/rank_players.sh stats.avg_starting_shanten_14 -p ./data/player -r ./data/rank

        bash ./ci/stats/rank_player_rates.sh win_rate -p ./data/player -r ./data/rank

        bash ./ci/stats/rank_player_rates.sh tsumo_rate -p ./data/player -r ./data/rank

        bash ./ci/stats/rank_player_rates.sh deal_in_rate -p ./data/player -r ./data/rank

        bash ./ci/stats/rank_player_rates.sh tsumo_against_rate -p ./data/player -r ./data/rank
        
        echo "Generated rankings:"
        ls -lh data/rank/ || echo "No rankings generated"
    
    - name: Generate history wins
      run: |
        cd tziakcha_record_miner
        
        bash ./ci/stats/history_wins.sh -p ./data/player/ -r ./data/rank/ -k 100
        
        echo "Generated history wins:"
        ls -lh data/rank/ || echo "No history wins generated"
    
    - name: Copy rankings to repository root
      run: |
        mkdir -p rank
        
        if [ -d "tziakcha_record_miner/data/rank" ]; then
          cp -rf tziakcha_record_miner/data/rank/* rank/ || true
          echo "Copied rankings"
        fi
        
        echo "Rank files:"
        ls -lh rank/ || echo "No rank files"
    
    - name: Configure Git
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
    
    - name: Commit and push changes
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        git add rank/
        
        if git diff --staged --quiet; then
          echo "No changes to commit"
          exit 0
        fi

        git commit -m "Auto-generate player rankings"

        git pull --rebase
        if [ $? -ne 0 ]; then
          echo "Rebase failed due to conflicts. Please resolve manually."
          exit 1
        fi

        git push
    
    - name: Cleanup
      if: always()
      run: |
        rm -rf tziakcha_record_miner
